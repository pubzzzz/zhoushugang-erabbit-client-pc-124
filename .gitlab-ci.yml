stages:
  - build

cache:
  key: ${CI_BUILD_REF_NAME}
  paths:
    - node_modules/
    - /home/erabbit-client-vue3/dist

build:
  stage: build
  only:
    - master
  script:
    - yarn
    - yarn run build
    - cp -r dist/* /home/erabbit-client-vue3/dist
  tags: 
    - erabbit-client-pc-vue3