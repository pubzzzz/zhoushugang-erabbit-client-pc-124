stages:
  - build

cache:
  key: ${CI_BUILD_REF_NAME}
  paths:
    - node_modules/

build:
  stage: build
  only:
    - master
  script:
    - cnpm install 
    - cnpm run build
    - cp -r dist/* /home/erabbit-client-vue3/dist
  tags: 
    - erabbit-client-pc-vue3